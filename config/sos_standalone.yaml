sos:
  standalone: true
  api:
    logger:
      level: debug
      format: text
    address:
      ip: 0.0.0.0
      port: 33221
  metadata_registry:
    db:
      type: mongodb
      host: mongodb://localhost:27017
      database: sos
      credentials:
        username: root
        password: root
      log_level: debug
  block_storage:
    db:
      type: leveldb
      host: mongodb://localhost:27017
      database: sos
      credentials:
        username: root
        password: root
      log_level: debug